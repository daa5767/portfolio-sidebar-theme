import { LitElement, html, css } from "lit";

class PortfolioScreen extends LitElement {
  static properties = {
    title: { type: String },
    name: { type: String },
    color: { type: String },
  };
}
